<h1 class="g-gray-9">Available CSS</h1>

<p class="g-gray-9">Here is a concat of all the CSS available to <a class="c-link" target="_blank" href="https://github.com/Ticketfly-UI/ticketfly-css">ticketfly-css</a> to make it easier to find classnames for development. This should be updated to an actual, organized reference eventually.</p>

<ul>
  {{#each model as |module|}}
    <li class="g-gray-7"><a class="c-link" href="#{{module.name}}">{{module.name}}</a></li>
  {{/each}}
</ul>

{{#each model as |module|}}
  <h2 class="g-gray-9" id={{module.name}}>{{module.name}}</h2>
  <pre class="c-pre g-bg-gray-0 u-p-md"><code>{{highlight module.body}}</code></pre>
{{/each}}

{{tf-button class="c-scroll-top-button" text="Scroll Top" click=(route-action "scrollTop")}}
