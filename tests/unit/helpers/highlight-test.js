
import { highlight } from 'ticketfly-css-doc/helpers/highlight';
import { module, test } from 'qunit';

module('Unit | Helper | highlight');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = highlight([42]);
  assert.ok(result);
});

